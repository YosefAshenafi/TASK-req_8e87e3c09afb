FROM node:20-alpine

# git + bash needed by some npm lifecycle scripts and husky
RUN apk add --no-cache git bash python3 make g++

WORKDIR /workspace

# Install dependencies first for Docker layer cache efficiency
COPY package*.json ./
RUN npm ci

# Copy all source files
COPY . .

# Never ship legacy ESLint config — image may have been built before .dockerignore excluded it
RUN rm -rf /workspace/.eslintrc.json

# Default command (overridden per-service in docker-compose)
CMD ["npm", "run", "test:unit"]
